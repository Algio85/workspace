{
  "name": "tao-workspace",
  "private": true,
  "scripts": {
    "build:tao": "npm run build --prefix packages/tao",
    "dev:portfolio": "npm run dev --prefix apps/portfolio",
    "build:portfolio": "npm run build --prefix apps/portfolio"
  }
}
